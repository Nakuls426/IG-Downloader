<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowActivityTransitions">true</item>
        <item name="android:fontFamily">@font/hind</item>
        <item name="actionMenuTextColor">@color/primaryTextWhite</item>
    </style>

    <style name="roundCustomButton" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/custom_button2</item>
        <item name="android:drawableStart">@drawable/ic_round_play_arrow</item>
        <item name="android:padding">12dp</item>
    </style>

    <style name="normalCustomButton1" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:textColor">@color/colorAccent</item>
        <item name="android:elevation">0dp</item>
        <item name="android:background">@drawable/custom_normal_button</item>
    </style>

    <style name="normalCustomButton2" parent="normalCustomButton1">
        <item name="android:textColor">@color/red</item>
        <item name="android:elevation">0dp</item>
        <item name="android:background">@drawable/custom_normal_button</item>
    </style>

    <style name="roundCustomButton.1" parent="Widget.AppCompat.Button.Borderless">
        <item name="android:elevation">10dp</item>
        <item name="android:background">@drawable/custom_button2</item>
        <item name="android:drawableStart">@drawable/ic_round_play_arrow</item>
        <item name="android:padding">18dp</item>
    </style>

    <style name="bottomNavigation" parent="Widget.Design.BottomNavigationView">
        <item name="android:state_selected">@drawable/item_background_state_image</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>
</resources>